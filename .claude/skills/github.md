# GitHub

Use the `gh` CLI tool for all GitHub operations instead of browser automation.

Examples:
- Create repos: `gh repo create`
- Create PRs: `gh pr create`
- View issues: `gh issue list`
- Clone repos: `gh repo clone`
